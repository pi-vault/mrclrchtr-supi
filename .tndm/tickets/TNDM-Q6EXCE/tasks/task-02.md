# Task 2: Task 2 (GREEN): Remove code_map from public tool surface

Remove `"code_map"` from `CODE_INTELLIGENCE_TOOL_NAMES` in `intent/types.ts`. Remove `code_map` spec entry from `tool/tool-specs.ts`. Remove `code_map` entry from `INTENT_GUIDELINES` in `tool/guidance.ts`.
