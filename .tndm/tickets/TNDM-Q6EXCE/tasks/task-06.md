# Task 6: Task 6: Final verification — typecheck, tests, stale reference scan

Run typecheck (`pnpm exec tsc -b`), run full test suite (`pnpm vitest run packages/supi-code-intelligence/`), scan for stale `code_map`/`executeMapTool`/`MapDetails` references across the package, verify Biome clean.
