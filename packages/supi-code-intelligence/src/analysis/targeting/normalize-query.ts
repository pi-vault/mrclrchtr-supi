/**
 * Query normalization — re-exported from the canonical targeting module.
 */

export type { QueryInput } from "../../targeting/query.ts";
export { normalizeQuery } from "../../targeting/query.ts";
