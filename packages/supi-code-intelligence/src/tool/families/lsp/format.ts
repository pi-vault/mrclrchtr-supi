/**
 * lsp_* family format — re-exports from the LSP format-utils module.
 */

export {
  diagnosticSeverityLabel,
  formatHover,
  formatLocation,
  formatSummaryEntry,
  symbolKindName,
} from "../../../lsp/format-utils.ts";
